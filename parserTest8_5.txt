//Environment hierarchy test file for flat hierarchy
//Expected output: 7 3 0
// global (parent) local (procedure)
// NOTE FOR MS. DATAR: THIS HAS BEEN MODIFIED BY ME TO WORK WITH THE NEW VARIABLE SYNTAX
//@author Tarush Gupta 
//@version 11/09/2025

VAR x, y, ignore;
PROCEDURE foo();
VAR y;
BEGIN
    x := 2;
    y := 7;
    ignore := bar();
    WRITELN(y);
END;
PROCEDURE bar();
VAR y;
BEGIN
    x := 3;
    y := y + 2;
END;
BEGIN
    x := 1;
    ignore := foo();
    WRITELN(x);
    WRITELN(y);
END;
.